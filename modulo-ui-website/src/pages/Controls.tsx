import styled from '@emotion/styled';
import Layout from '../components/Layout';
import StyledTable from '../components/StyledTable';
import { ToggleSwitch, Range } from 'modulo-ui';

const Controls = () => {

    return (
    <Layout>
    <ToggleSwitch />
    <Range />
    </Layout>
    )
}
export default Controls
